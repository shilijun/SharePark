<!--pages/records/records.wxml-->
<view class="container">
  <block wx:if="{{rendRecords.length}}">
    <swiper class='record-swiper' indicator-dots='{{true}}'
      previous-margin="50rpx" next-margin="50rpx"
      current='0'>
      <swiper-item class='movie' wx:for="{{rendRecords}}">
        <myCard type='{{item.type}}' addr='{{item.addr}}' lockid="{{item.lockid}}"  time="{{item.starttime}}" unitPrice="{{item.unitPrice}}"></myCard>
      </swiper-item>
   </swiper>
  </block>

    <!-- <view class="item" wx:for="{{rendRecords}}" catchtap="seeDetail" data-id="{{index}}">
      <text>{{item.renderid}}</text>
      <text>{{item.tenantid}}</text>
      <text>{{item.starttime}}</text>
      <text>{{item.endtime}}</text>
    </view> -->

    <!-- <view class="item" wx:for="{{mrendRecords}}" catchtap="seeDetail" data-id="{{index}}">
      <text>{{item.renderid}}</text>
      <text>{{item.tenantid}}</text>
      <text>{{item.starttime}}</text>
      <text>{{item.endtime}}</text>
    </view>  -->
  <block wx:else>
    <view class="empty">
      <text class="content">您没用租用车位或正在被租用车位</text>
    </view>
  </block>
</view>
