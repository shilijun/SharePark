<!--pages/records/records.wxml-->
<view class="container">
<view class="item" wx:for="{{records}}" catchtap="seeDetail" data-id="{{index}}">
  <text>{{item.start}}</text>
  <text>{{item.end}}</text>
  <text>{{item.money}}</text>
</view>
</view>
